version: "3"
services:
  database:
    image: mariadb:10.4
    container_name: padron_afip_db
    restart: unless-stopped
    volumes:
      - ./data:/var/lib/mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: user
      MYSQL_ROOT_PASSWORD: pass
